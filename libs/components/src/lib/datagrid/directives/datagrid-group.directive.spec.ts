import { DatagridGroupDirective } from './datagrid-group.directive';

describe('DatagridGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new DatagridGroupDirective();
    expect(directive).toBeTruthy();
  });
});
