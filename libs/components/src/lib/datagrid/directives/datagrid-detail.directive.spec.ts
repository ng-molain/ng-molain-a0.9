import { DatagridDetailDirective } from './datagrid-detail.directive';

describe('DatagridDetailDirective', () => {
  it('should create an instance', () => {
    const directive = new DatagridDetailDirective();
    expect(directive).toBeTruthy();
  });
});
