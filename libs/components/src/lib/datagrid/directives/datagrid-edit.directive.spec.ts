import { DatagridEditDirective } from './datagrid-edit.directive';

describe('DatagridEditDirective', () => {
  it('should create an instance', () => {
    const directive = new DatagridEditDirective();
    expect(directive).toBeTruthy();
  });
});
