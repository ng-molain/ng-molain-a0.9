import { ItemTemplateDirective } from './item-template.directive';

describe('ItemTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
