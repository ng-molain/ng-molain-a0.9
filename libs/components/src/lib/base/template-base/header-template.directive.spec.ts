import { HeaderTemplateDirective } from './header-template.directive';

describe('HeaderTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
