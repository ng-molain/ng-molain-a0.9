import { PageTemplateDirective } from './page-template.directive';

describe('PageTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new PageTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
