import { DetailTemplateDirective } from './detail-template.directive';

describe('DetailTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new DetailTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
