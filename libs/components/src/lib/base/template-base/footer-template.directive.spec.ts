import { FooterTemplateDirective } from './footer-template.directive';

describe('FooterTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new FooterTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
