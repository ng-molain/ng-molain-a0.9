import { FilterTemplateDirective } from './filter-template.directive';

describe('FilterTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
