import { EditTemplateDirective } from './edit-template.directive';

describe('EditTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new EditTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
