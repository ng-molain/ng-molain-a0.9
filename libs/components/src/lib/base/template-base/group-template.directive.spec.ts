import { GroupTemplateDirective } from './group-template.directive';

describe('GroupTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new GroupTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
