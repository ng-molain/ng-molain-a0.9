import { BodyTemplateDirective } from './body-template.directive';

describe('BodyTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new BodyTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
