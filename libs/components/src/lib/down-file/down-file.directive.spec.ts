import { DownFileDirective } from './down-file.directive';

describe('DownFileDirective', () => {
  it('should create an instance', () => {
    // const directive = new DownFileDirective();
    // expect(directive).toBeTruthy();
  });
});
