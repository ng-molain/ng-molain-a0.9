import { GridHeaderDirective } from './grid-header.directive';

describe('GridHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new GridHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
